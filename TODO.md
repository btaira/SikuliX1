# TODO

- [x] Make `Stop` interrupt `Pause` and delay-style `Wait/Delay` immediately during workflow/code execution.
- [x] Fix `Capture` so a selection in `capture.html` reliably creates a template in the Templates panel.
- [x] Fix `Record` so recorded clicks reliably append workflow steps in real browser usage.
- [x] Re-test template actions (`+ Step`, delete) after capture is fixed.
